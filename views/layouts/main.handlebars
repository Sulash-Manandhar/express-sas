<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>{{title}}</title>
    <style>
      body { margin: 0; padding: 2rem; position: relative; font-family: "Arial";
      text-align: center; color: #646464; } h1, h2, h3, h4, h5, h6 { color:
      #373737; margin-bottom: 0; } b { color: #373737; font-weight: bolder; }
      .btn { padding: 8px 16px; color: white; font-weight: bold; font-size:
      16px; background-color: #9747ff; border: none; border-radius: 4px; }
      .btn:hover { background-color: #8f00ff; }
    </style>
  </head>
  <body>
  {{>header}}
  {{{body}}}
  {{> footer}}
  </body>
</html>